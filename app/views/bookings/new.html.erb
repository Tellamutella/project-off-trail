      <div class="container">
        <div class="row">
          <div class="col-md-6 col-md-offset-3">
            <h2>New booking for <%= @location.name %></h2>
            <%= simple_form_for [@location, @booking] do |f| %>
            <%= f.input :date %>
            <%= f.submit "Confirm Booking", class: "btn btn-danger" %>
            <% end %>
          </div>
        </div>
      </div>
